{
  "name": "@nodebyte/games",
  "version": "0.1.0",
  "description": "Standalone CI and validation helpers for NodeByte game recipes.",
  "license": "AGPL-3.0-only",
  "scripts": {
    "validate:recipes": "node scripts/validate-games.mjs",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check",
    "lint": "deno lint",
    "typecheck": "node scripts/typecheck.mjs",
    "ci": "npm run fmt:check && npm run lint && npm run validate:recipes && npm run typecheck"
  }
}
