{
  "fmt": {
    "lineWidth": 100,
    "indentWidth": 2,
    "semiColons": true,
    "singleQuote": false,
    "include": ["**/*.ts", "scripts/**/*.mjs"],
    "exclude": [".git/**", ".github/**"]
  },
  "lint": {
    "rules": {
      "exclude": ["no-process-global"],
      "tags": ["recommended"]
    },
    "include": ["**/*.ts", "scripts/**/*.mjs"],
    "exclude": [".git/**", ".github/**"]
  }
}
